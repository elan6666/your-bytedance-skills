interface:
  display_name: "Your ByteDance Build"
  short_description: "Implement focused changes and verify the result."
  default_prompt: "Use $byte-build to implement this request with focused changes and proportionate verification."
