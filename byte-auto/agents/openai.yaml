interface:
  display_name: "Your ByteDance Auto"
  short_description: "Run from idea to delivery, using safe subagent parallelism when useful."
  default_prompt: "Use $byte-auto to take this product idea to a deliverable product. Decide whether subagents can safely parallelize the work, then keep looping until plans are complete, review passes, delivery exists, and verification is recorded."
