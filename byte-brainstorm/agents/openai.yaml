interface:
  display_name: "Your ByteDance Brainstorm"
  short_description: "Explicit-only idea expansion outside the workflow."
  default_prompt: "Use $byte-brainstorm to expand this rough idea into multiple product directions, bold alternatives, MVP shapes, and decision options without entering the normal workflow."
